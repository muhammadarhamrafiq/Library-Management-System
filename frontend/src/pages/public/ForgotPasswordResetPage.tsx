import { PageStub } from "@/components/layout/PageStub"
export default function ForgotPasswordResetPage() {
  return <PageStub title="Forgot password — new password" note="Phase 7 — calls POST /reset-password/confirm" />
}
