import { PageStub } from "@/components/layout/PageStub"
export default function RegisterPage() {
  return <PageStub title="Register — step 1" note="Phase 6 — details form, calls POST /register/initiate" />
}
