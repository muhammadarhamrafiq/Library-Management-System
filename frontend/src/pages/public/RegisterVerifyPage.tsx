import { PageStub } from "@/components/layout/PageStub"
export default function RegisterVerifyPage() {
  return <PageStub title="Register — verify OTP" note="Phase 6 — OTP input, calls POST /register/verify" />
}
