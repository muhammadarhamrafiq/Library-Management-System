import { PageStub } from "@/components/layout/PageStub"
export default function ForgotPasswordPage() {
  return <PageStub title="Forgot password — step 1" note="Phase 7 — email form, calls POST /reset-password/initiate" />
}
