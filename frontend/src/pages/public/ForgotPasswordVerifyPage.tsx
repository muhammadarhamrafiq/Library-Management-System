import { PageStub } from "@/components/layout/PageStub"
export default function ForgotPasswordVerifyPage() {
  return <PageStub title="Forgot password — verify OTP" note="Phase 7 — OTP input, calls POST /reset-password/verify" />
}
